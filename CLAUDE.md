# CLAUDE.md — Invoice Rescue

## Production D1 workflow

Schema lives in `backend/db/migrations/*.sql`, tracked via `wrangler d1 migrations` (no more ad-hoc `d1 execute --file=...`). To change the schema: `npx wrangler d1 migrations create invoice-rescue-db <name>`, edit the generated file, verify with `npx wrangler d1 migrations apply invoice-rescue-db --local` (wipes `.wrangler/state` first if you want a truly clean test), then apply for real with `--remote`. D1/wrangler has no automatic "down" migration — to roll back, write and apply a new forward migration that undoes the change; since SQLite can't `ALTER TABLE DROP CONSTRAINT`, undoing a CHECK-constraint migration means rebuilding the table again (see `0003_add_check_constraints.sql` for the rename/create/copy/drop pattern used here). Beyond that, D1 has automatic **Time Travel** for point-in-time recovery — no setup required, `npx wrangler d1 time-travel restore invoice-rescue-db --bookmark=<bookmark>` or `--timestamp=<ISO8601>` — plus manual snapshots via `npx wrangler d1 export invoice-rescue-db --remote --output=backup.sql`. Secrets (`GEMINI_API_KEY`, `STRIPE_SECRET_KEY`) belong in Cloudflare's secret store via `npx wrangler secret put <NAME>`, never in `wrangler.jsonc` vars or anything committed; `.dev.vars` (gitignored) holds local-dev copies only.

## Client portal + Stripe billing (added 2026-08-13)

Clients now get their own login at `/portal` — no public signup: a client record must already exist (created by the operator via `POST /api/clients`, as before) with a `contact_email`. Login is a magic link, not a password: `POST /portal/login` emails a 15-minute HMAC-signed token via the `SEND` binding, `GET /portal/verify` exchanges it for a 7-day HttpOnly session cookie (`PORTAL_SESSION_SECRET`, see `backend/src/lib/portal-auth.ts`). No JWT library, no password storage/reset flow — deliberately, both to stay dependency-free and because a password reset flow is more attack surface than a solo operator needs for 2-3 clients.

`/portal/dashboard` shows each client only their own invoices and chase history (scoped by the session's client id, never a client-supplied id) — including who reviewed and approved each sent message and when (`chase_log.reviewed_by`/`reviewed_at`). This audit trail is the deliberate differentiator: competitor research (Chaser, Satago, Upflow, Kolleno, Esker, Sidetrade, etc.) shows every AI-native competitor moving toward *less* human involvement per message to protect margin at scale, and none productizes "a human read this before it sent" as a client-visible trust feature — see the chat history around 2026-08-13 for the full comparison. Public self-serve signup was deliberately skipped: it isn't a competitive requirement at this price tier (£349-£1,250/mo) and doesn't fit the 2-3 client, low-maintenance stage docs/credit-control-system-design.md targets.

Billing: `POST /api/clients` now best-effort creates a Stripe customer (`backend/src/lib/stripe.ts` — plain `fetch()` calls, no `stripe-node` dependency) and stores `clients.stripe_customer_id`; `POST /portal/billing` mints a Stripe-hosted billing-portal session and redirects there. `POST /api/billing/webhook` verifies Stripe's signature and syncs `clients.status` from subscription events. Full flow (login → session → dashboard → real Stripe test-mode billing-portal redirect → logout) verified end-to-end against local D1 + `wrangler dev` on 2026-08-13, both via `backend/test/rest-api.sh` and a real browser click-through.

**Legal note, not legal advice:** `frontend/terms/index.html` already states Invoice Rescue is not a debt collection agency and not FCA-regulated; `frontend/privacy/index.html` already discloses Stripe as a processor. Neither was changed — a client logging in to view their own data and manage billing through Stripe's own hosted portal doesn't itself change that regulatory posture. That said, this determination hasn't been reviewed by a solicitor as part of this change; get one to confirm it periodically, especially before adding anything that looks like acting on debts owed *to* Invoice Rescue itself, or chasing consumer (not B2B) debtors.

## Known gaps (as of 2026-08-13)

- **`STRIPE_WEBHOOK_SECRET` is set locally (`.dev.vars`) but not yet in production** — it can't be, until the Stripe webhook endpoint below exists (Stripe only issues the signing secret once you create the endpoint). `PORTAL_SESSION_SECRET` and `ADMIN_SECRET` ARE now set in production (via `wrangler secret put` from a `wrangler login` session, 2026-08-13; values recorded in `D:\Dev\Secrets` — see `INDEX.md`). `verifyWebhookSignature()` in `stripe.ts` explicitly rejects a missing secret rather than silently verifying against an empty one, so `/api/billing/webhook` 400s (fails closed) until the endpoint + secret exist — confirmed live on 2026-08-13.
- **Stripe webhook endpoint doesn't exist yet in the Stripe Dashboard.** `/api/billing/webhook` is live in code but nothing calls it until you create the endpoint (Developers → Webhooks → Add endpoint → `https://invoicerescue.co.uk/api/billing/webhook`, events `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.payment_failed`) and copy its signing secret into `STRIPE_WEBHOOK_SECRET` above. I didn't create this myself — creating a webhook endpoint changes your live Stripe account configuration, which isn't something to do without you present.
- **Stripe Billing Portal needs one manual Dashboard configuration change before it's safe to link publicly: disable self-serve subscription cancellation.** `frontend/terms/index.html` §"Cancellation" commits Engine/Operator clients to a 3-month minimum term, cancelled by emailing `hello@invoicerescue.co.uk` — Stripe's default portal configuration lets a customer cancel immediately, which would contradict that. Settings → Billing → Customer portal → turn off "Cancel subscriptions" (or restrict it to your terms), leave "update payment method" and "view invoices" on. Same reasoning as the webhook endpoint above: an account-settings change, left for you to make.
- **`STRIPE_SECRET_KEY` in production is still the test-mode key** (per the existing comment in `wrangler.jsonc`, set 2026-07-20). The new customer-creation and billing-portal code work end-to-end against it (verified locally), but no real card will be charged until you deliberately swap in the live secret key — that's a go-live decision, not something to flip silently.
- **No rate limiting on `/portal/login`.** Fine at the 2-3 client scale docs/credit-control-system-design.md targets (a flood of magic-link requests just means a flood of harmless emails — the response never reveals whether the address matched), but worth adding if this ever fronts more than a handful of clients.

- **Deploy token can read but not write secrets.** The `CLOUDFLARE_API_TOKEN` in `.dev.vars`/`D:\Dev\Secrets\Cloudflare-api-token.txt` (Cloudflare token id `bffb5304a486f8713696516a5f6cb9db`, named "invoice-rescue-agent") verifies fine and — despite the earlier note here — **can** list Workers scripts, fetch worker source, and list D1 databases; its permission grant does include `Workers Scripts Write`. But a live test of `PATCH /accounts/<id>/workers/scripts/invoice-rescue/secrets` (the bulk secrets endpoint) with this token returns `{"code":10405,"message":"Method not allowed for this authentication scheme"}` — this specific write path rejects API-token auth outright, permission groups notwithstanding. Deploys apparently go through fine via a different path: the live Worker (`modified_on` 2026-08-09, `last_deployed_from: "wrangler"`) already matches `backend/src/index.ts` — confirmed by diffing the deployed bundle, which includes `requireAdminAuth()`, `POST /api/clients`, and all four gated routes below. That deploy almost certainly came from a `wrangler login` browser-authenticated session on T's machine, not this static token. **Fix for secrets specifically:** run `npx wrangler secret put ADMIN_SECRET` (and any future secret) from a `wrangler login` session — don't rely on the static `CLOUDFLARE_API_TOKEN` for secret writes, it's rejected at the API level regardless of scope.
- **RESOLVED 2026-08-13: `ADMIN_SECRET` is now set in production** (was previously never set — see git history for this file if you need the original writeup of that gap). Verified live: `GET /admin` with the new credential returns 200, without it 401. Value is in `D:\Dev\Secrets\Invoice-Rescue-Admin-Secret.txt`. Still a stopgap, not real auth: one shared password, no rotation, no audit log.
- **RESOLVED 2026-08-24: Cloudflare Access now sits in front of `/admin`, `/api/chase/*`, and `/api/clients*`** (three self-hosted Access apps — `invoicerescue.co.uk/admin`, `invoicerescue.co.uk/api/chase/*`, `invoicerescue.co.uk/api/clients*`, the last covering the CSV import route too via the trailing wildcard). Allow policy: email OTP for `tiborcc2@gmail.com` / `hello@invoicerescue.co.uk` / `tibor@invoicerescue.co.uk`, same pattern as the existing `ops-mcp.invoicerescue.co.uk` Access app. Verified live: unauthenticated `curl` to all four paths now gets a 302 to Access login instead of a 200/reaching the Worker; `/api/health` deliberately left open. `requireAdminAuth()`'s Basic Auth stays in place underneath as defense in depth — not redundant to remove.
- **Unused secret/var bindings in production worth a cleanup pass:** `CF_API_TOKEN`, `S3_COMPATIBLE_R2_API_S3_CLIENT`, `S3_ENDPOINT`, `SECRET_ACCESS` are all set as `secret_text` bindings on the deployed Worker but aren't referenced anywhere in `backend/src/index.ts` or its libs — looks like leftover R2/API-token values that got added as Worker secrets instead of staying in `.dev.vars`/`D:\Dev\Secrets`. Not blocking anything, but worth removing (`npx wrangler secret delete <NAME>`) since unused secrets on a Worker are just extra surface area.
